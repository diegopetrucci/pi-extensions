# Announcement drafts — v0.1.68

## Short

Dynamic Context Pruning now defaults to a conservative `mid_loop=1` break-even threshold while idle stays `22`, and status shows both effective thresholds. Manual prunes, persisted decisions, and saved overrides are unchanged. The cache-cost explanation now distinguishes measured corpus results from universal claims. Fleet certification remains Pi 0.84.4.

## Packages

- `pi-dynamic-context-pruning@0.1.9`
- `@diegopetrucci/pi-extensions@0.1.68`

<!-- prepare-release:packages [["pi-dynamic-context-pruning","0.1.9"],["@diegopetrucci/pi-extensions","0.1.68"]] -->
