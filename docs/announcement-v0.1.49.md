# Announcement — v0.1.49

Released `@diegopetrucci/pi-extensions@0.1.49` and `@diegopetrucci/pi-librarian@0.1.6`.

This release fixes [#1](https://github.com/diegopetrucci/pi-extensions/issues/1): `librarian` now defaults to low thinking, prefers fast/light model families, removes `model=current`, and prompts its internal scout to batch independent GitHub probes in parallel.

## Short post

Released `pi-extensions` v0.1.49 with faster `librarian` defaults: low thinking, fast/light model preference, no current-model inheritance, and parallel probe guidance.

## Tiny post

Released `pi-extensions@0.1.49` and `pi-librarian@0.1.6`: faster Librarian defaults with low thinking and no current-model inheritance. Fixes #1.
